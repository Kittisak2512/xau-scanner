fastapi==0.110.0
uvicorn==0.27.0.post1

# สำหรับคำนวณ/สัญญาณ
pandas==2.2.2
numpy==1.26.4

# เรียก TwelveData / HTTP
requests==2.31.0

# สำหรับอัพโหลดไฟล์ (FastAPI ต้องใช้)
python-multipart==0.0.9

# ตัวช่วย build บน Render (สำคัญที่สุด)
setuptools==68.2.2
wheel==0.41.2
